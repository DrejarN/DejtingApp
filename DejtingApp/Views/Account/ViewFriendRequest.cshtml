@model IEnumerable<DejtingApp.Models.FriendRequestViewModel>

@{
    ViewBag.Title = "Your pending friendrequests";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Your pending friendrequests</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Förnamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Efternamn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Förnamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Efternamn)
        </td>
        <td>
            @Html.ActionLink("Accept", "AcceptFriendRequest", "Account", new { item.ProfileId }, null)
            @*@Html.ActionLink("Delete", "DeclineFriendRequest", null, new { id = "delete" })*@
            @Html.ActionLink("Decline", "DeclineFriendRequest", "Account", new { item.ProfileId }, null)
            @*<button type="submit" onclick="return DeclineFriendRequest();"></button>*@
        
            @*<script type="text/javascript">
                $("#delete").click(function (e) {

                    e.preventDefault();
                    $.ajax({
                        url: $(this).attr("href"),
                        //type: 'POST',
                        data: {id = @item.ProfileId},
                        success: function () {
                            alert("Value Added");
                        }

                    });

                });

            </script>*@
        </td>
    </tr>
}

</table>